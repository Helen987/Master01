USE [BI_DM]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO








CREATE VIEW [dbo].[V_CURRENCY_RATE]
AS SELECT
    [VALID_FROM_YMD],
    [CURRENCY_CD],
    [RATE_DIV],
    [RATE]
FROM OPENQUERY([WA], 'SELECT * FROM UWA00001.R_CURRENCY_RATE WHERE RATE_DIV = ''A''')









GO
